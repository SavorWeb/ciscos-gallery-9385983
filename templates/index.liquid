</div>
<div class="index-bg">
  {% if settings.slideshow %}

  <!-- slider css -->
  <style>
    .visibleNearby {
      width: 100%;
      background: #fff;
      color: #FFF;
      padding-top: 25px;
    }
    .visibleNearby .rsGCaption {
      font-size: 16px;
      line-height: 18px;
      padding: 12px 0 16px;
      background: #fff;
      width: 100%;
      position: static;
      float: left;
      left: auto;
      bottom: auto;
      text-align: center;
    }
    .visibleNearby .rsGCaption span {
      display: block;
      clear: both;
      color: #bbb;
      font-size: 14px;
      line-height: 22px;
    }


    /* Scaling transforms */
    .visibleNearby .rsSlide img {
      opacity: 0.45;
      -webkit-transition: all 0.3s ease-out;
      -moz-transition: all 0.3s ease-out;
      transition: all 0.3s ease-out;

      -webkit-transform: scale(0.9);  
      -moz-transform: scale(0.9); 
      -ms-transform: scale(0.9);
      -o-transform: scale(0.9);
      transform: scale(0.9);
    }
    .visibleNearby .rsActiveSlide img {
      opacity: 1;
      -webkit-transform: scale(1);  
      -moz-transform: scale(1); 
      -ms-transform: scale(1);
      -o-transform: scale(1);
      transform: scale(1);
    }

    /* Non-linear resizing on smaller screens */
    @media screen and (min-width: 0px) and (max-width: 900px) { 
      #gallery-1 {
        padding: 12px 0 12px;
      }
      #gallery-1 .rsOverflow,
      .royalSlider#gallery-1 {
        height: 200px !important;
      }
    }
    @media screen and (min-width: 0px) and (max-width: 500px) { 
      #gallery-1 .rsOverflow,
      .royalSlider#gallery-1 {
        height: 150px !important;
      }
    }

  </style>


<div class="desktop-slider slide-wrap"><div id="gallery-1" class="royalSlider rsDefault visibleNearby">
  {% if settings.display-sl1 %}
    <div><a href="{{ settings.sl1-link }}"><img src="{{ 'sl1-image.jpg' | asset_url }}" alt="{{ settings.sl1-alt }}"></a></div>
  {% endif %}
  {% if settings.display-sl2 %}
    <div><a href="{{ settings.sl2-link }}"><img src="{{ 'sl2-image.jpg' | asset_url }}" alt="{{ settings.sl2-alt }}"></a></div>
  {% endif %}
  {% if settings.display-sl3 %}
    <div><a href="{{ settings.sl3-link }}"><img src="{{ 'sl3-image.jpg' | asset_url }}" alt="{{ settings.sl3-alt }}"></a></div>
  {% endif %}
  {% if settings.display-sl4 %}
    <div><a href="{{ settings.sl4-link }}"><img src="{{ 'sl4-image.jpg' | asset_url }}" alt="{{ settings.sl4-alt }}"></a></div>
  {% endif %}
  {% if settings.display-sl5 %}
    <div><a href="{{ settings.sl5-link }}"><img src="{{ 'sl5-image.jpg' | asset_url }}" alt="{{ settings.sl5-alt }}"></a></div>
  {% endif %}
  {% if settings.display-sl6 %}
    <div><a href="{{ settings.sl6-link }}"><img src="{{ 'sl6-image.jpg' | asset_url }}" alt="{{ settings.sl6-alt }}"></a></div>
  {% endif %}
</div></div>

<div class="mobile-slider slide-wrap"><div id="gallery-2" class="royalSlider rsDefault visibleNearby">
  {% if settings.display-sl1 %}
    <a class="rsImg" href="{{ 'sl1-image.jpg' | asset_url }}" alt="{{ settings.sl1-alt }}"></a>
  {% endif %}
  {% if settings.display-sl2 %}
    <a class="rsImg" href="{{ 'sl2-image.jpg' | asset_url }}" alt="{{ settings.sl2-alt }}"></a>
  {% endif %}
  {% if settings.display-sl3 %}
    <a class="rsImg" href="{{ 'sl3-image.jpg' | asset_url }}" alt="{{ settings.sl3-alt }}"></a>
  {% endif %}
  {% if settings.display-sl4 %}
    <a class="rsImg" href="{{ 'sl4-image.jpg' | asset_url }}" alt="{{ settings.sl4-alt }}"></a>
  {% endif %}
  {% if settings.display-sl5 %}
    <a class="rsImg" href="{{ 'sl5-image.jpg' | asset_url }}" alt="{{ settings.sl5-alt }}"></a>
  {% endif %}
  {% if settings.display-sl6 %}
    <a class="rsImg" href="{{ 'sl6-image.jpg' | asset_url }}" alt="{{ settings.sl6-alt }}"></a>
  {% endif %}
</div></div>

{% endif %}

<div class="container">
<!--   {% if settings.frontpage-collection != blank and settings.display-featured-product %}

  <div class="content-heading">
    <h4 class="p0 m0">{% unless settings.fp-coll-heading == blank %} {{settings.fp-coll-heading}} {% else %} Featured {% endunless %}</h4>

    {% unless settings.fp-coll-sub-heading == blank %}
    <p class="p0 m0">
      {{settings.fp-coll-sub-heading}}
    </p>
    {% endunless %}
  </div>

  <ul class="grid">
    <div class="loader center">{{ 'loading.GIF' | asset_url | img_tag }}</div>
    {% for product in collections[settings.frontpage-collection].products %}
    {% include 'pin' %}
    {% endfor %}
  </ul>

  {% endif %} -->

<!--   {% if settings.display-featured-blog %} 
  <div class="content-heading">
    <h4 class="p0 m0">{% unless settings.fp-blog-heading == blank %} {{settings.fp-blog-heading}} {% else %} From our blog {% endunless %}</h4>
    
    {% unless settings.fp-blog-sub-heading == blank %}
    <p class="p0 m0">
      {{ settings.fp-blog-sub-heading }}
    </p>
    {% endunless %}
  </div>

  {% if blogs.[settings.featured-blog].articles.size > 0 %}
  <div class="latest">
    <div class="row">
      {% for article in blogs.[settings.featured-blog].articles limit: 3 %}
      <div class="col-xs-12 col-sm-4">
        <section>
          {{ article.excerpt_or_content }}
        </section>
        <h4>{{ article.title }}</h4>
        <span>{{ article.published_at | date: "%B. %d. %Y" }}</span>
        <a href="{{ article.url }}" class="button clr3">Read more &rarr;</a>
      </div>
      {% endfor %}
    </div>
  </div>
  {% else %}
  <p>The blog does not have any content</p>
  {% endif %}
  {% endif %} -->

  <div class="main-row row">
<!--     {% if settings.display-welcome-message %} -->
    <div class="welcome-message col-xs-12 col-sm-3">
      <div class="content-heading">
        <h4 class="p0 m0">{{ settings.welcome-title }}</h4>
      </div>

      {{ settings.welcome-content }}
    </div>
    <div class="feature-box col-xs-12 col-sm-7">
      <div class="inner">
        <h2 class="m0">{{ settings.main-box-title }}</h2>
        
        <a href="{{ settings.main-box-link }}"><img src="{{ 'main-box-img.jpg' | asset_url }}" alt=""></a>

      </div>
    </div>
    <div class="feature-box col-xs-12 col-sm-2">
     <div class="inner small-box">
        <a href="{{ settings.second-box-link }}"><img src="{{ 'second-box-img.jpg' | asset_url }}" alt=""></a>
        <h2 class="m0">{{ settings.second-box-title }}</h2>
      </div>
     <div class="inner small-box">
        <a href="{{ settings.third-box-link }}"><img src="{{ 'third-box-img.jpg' | asset_url }}" alt=""></a>
        <h2 class="m0">{{ settings.third-box-title }}</h2>
      </div>
  {% if settings.display-box-four %} 
     <div class="inner small-box">
        <a href="{{ settings.fourth-box-link }}"><img src="{{ 'fourth-box-img.jpg' | asset_url }}" alt=""></a>
        <h2 class="m0">{{ settings.fourth-box-title }}</h2>
      </div>
  {% endif %}
    </div>
<!-- 
    <div class="category-icons col-xs-12 col-sm-12 clearfix">
      <div class="flourish"><img src="{{ 'flourish.jpg' | asset_url }}" alt=""></div>
      <div class="category-avatar"><a href="/collections/native-american/"><img src="{{ 'cat1.jpg' | asset_url }}" alt=""></a></div>
      <div class="category-avatar"><a href="/collections/cowboy-and-western/"><img src="{{ 'cat2.jpg' | asset_url }}" alt=""></a></div>
      <div class="mid-ava category-avatar"><a href="/collections/fine-art/"><img src="{{ 'cat3.jpg' | asset_url }}" alt=""></a></div>
      <div class="category-avatar"><a href="/collections/jewelry/"><img src="{{ 'cat4.jpg' | asset_url }}" alt=""></a></div>
      <div class="category-avatar"><a href="/collections/sporting-goods/"><img src="{{ 'cat5.jpg' | asset_url }}" alt=""></a></div>
      <div class="category-avatar"><a href="/collections/lodge-furnishings/"><img src="{{ 'cat6.jpg' | asset_url }}" alt=""></a></div>
    </div> -->

<!--     <div class="hidden-xs col-sm-5 col-sm-offset-1">
      {% else %}
      <div class="hidden-xs col-sm-5">
        {% endif %}
        {% if settings.display-timeline %}
        <div class="content-heading">
          <h4 class="p0 m0">Tweets</h4>
        </div>


        <div class="intrinsic-wrap">
          {% unless settings.twitter-username == blank %}
          {% assign twitter-username = settings.twitter-username %}
          {% else %}
          {% assign twitter-username = "shopify" %}
          {% endunless %}

          <a class="twitter-timeline" data-dnt="true" href="https://twitter.com/{{ twitter-username }}" data-screen-name="{{ twitter-username }}" data-widget-id="408124053057048576" data-chrome="noborder noheader nofooter transparent">Tweets by @{{ twitter-username }}</a>
          <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+"://platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs);}}(document,"script","twitter-wjs");</script>
        </div>

        {% endif %}

      </div>
    </div> -->

    <script id="addJS">// Important note! If you're adding CSS3 transition to slides, fadeInLoadedSlide should be disabled to avoid fade-conflicts.
      jQuery(document).ready(function($) {

        var si = $('#gallery-1').royalSlider({
          addActiveClass: true,
          autoScaleSlider: false, 
          autoScaleSliderWidth: 920,     
          autoScaleSliderHeight: 260,
          loop: true,
          fadeinLoadedSlide: false,
          globalCaption: true,
          keyboardNavEnabled: true,
          globalCaptionInside: false,
          imageScaleMode: 'fill',
          imageScalePadding: 0,
          numImagesToPreload: 6,
          imageAlignCenter: true,
          transitionSpeed: {{ settings.speed }},

            visibleNearby: {
              enabled: true,
              centerArea: 0.7,
              center: true,
              breakpoint: 650,
              breakpointCenterArea: 0.64,
              navigateByCenterClick: true
            },

          autoPlay: {
            enabled: {{ settings.autoplay }},
            pauseOnHover: true,
            stopAtAction: true,
            delay: {{ settings.delay }}
          }

        }).data('royalSlider');

        var si = $('#gallery-2').royalSlider({
          addActiveClass: true,
          arrowsNav: true,
          controlNavigation: 'none',
          autoScaleSlider: false, 
          autoScaleSliderWidth: 1000,     
          autoScaleSliderHeight: 100,
          loop: true,
          fadeinLoadedSlide: false,
          globalCaption: true,
          keyboardNavEnabled: true,
          globalCaptionInside: false,
          imageScalePadding: 0,
          numImagesToPreload: 6,
          transitionSpeed: {{ settings.speed }},

            visibleNearby: {
              enabled: false,
              centerArea: 0.7,
              center: true,
              breakpoint: 650,
              breakpointCenterArea: 0.64,
              navigateByCenterClick: true
            },

          autoPlay: {
            enabled: {{ settings.autoplay }},
            pauseOnHover: true,
            stopAtAction: true,
            delay: {{ settings.delay }}
          }

        }).data('royalSlider');

      });
    </script>
<!--
<script>
  ;(function() {
    if ($.browser.mobile)
    {
      var flex = $('.featured-flex').flexslider({
        prevText: "",
        nextText: "",  
        directionNav: false,
        animation: "slide"
      });
    }
    else
    {
    var flex = $('.featured-flex').flexslider({
        prevText: "",
        nextText: "",  
        directionNav: true
      });
    }
  })();
</script>-->
</div>

<style>
  footer {
    margin-bottom: -50px;
  }
</style>